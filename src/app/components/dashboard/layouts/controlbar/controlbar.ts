import { Component, Injector, ViewEncapsulation } from '@angular/core';

@Component({
    templateUrl: './controlbar.html',
    selector: 'control-bar',
    encapsulation: ViewEncapsulation.None
})

export class Controlbar {
    constructor() {

    }
}